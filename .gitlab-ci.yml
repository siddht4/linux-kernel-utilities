image: ubuntu

before_script:
  - RUN add-apt-repository -y ppa:duggan/bats
  - RUN apt-add-repository -y ppa:ubuntu-toolchain-r/test
  - RUN apt-add-repository -y ppa:beineri/opt-qt56-trusty
  - RUN apt-get -qq update
  - RUN apt-get -qq install bats gcc make fakeroot libncurses5 libncurses5-dev kernel-package build-essential pkg-config libnotify-bin gnupg qt56tools qt56svg qt56script

bats:
  script:
    - bats ./unitTesting/tests-linux-kernel-utilities.bats
    - source /opt/qt56/bin/qt56-env.sh
